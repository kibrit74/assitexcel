<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<OfficeApp xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
  xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
  xsi:type="TaskPaneApp">
  
  <!-- Begin Basic Settings: Add-in metadata, used for all versions of Office unless override provided. -->
  <Id>a1b2c3d4-e5f6-7890-abcd-ef1234567890</Id>
  <Version>1.0.0.0</Version>
  <ProviderName>ExcelBot AI</ProviderName>
  <DefaultLocale>tr-TR</DefaultLocale>
  
  <!-- The display name of your add-in. Used on the store and various places of the Office UI such as the add-ins dialog. -->
  <DisplayName DefaultValue="ExcelBot AI Formül Yardımcısı" />
  <Description DefaultValue="AI destekli Excel formül ve makro yardımcısı"/>
  
  <!-- Icon for your add-in. Used on installation screens and the add-ins dialog. -->
  <IconUrl DefaultValue="https://localhost:5173/icon-32.png" />
  <HighResolutionIconUrl DefaultValue="https://localhost:5173/icon-64.png" />
  
  <!-- Support URL -->
  <SupportUrl DefaultValue="https://localhost:5173/help" />
  
  <!-- Domains that will be allowed when navigating. For this template, only one domain is used and it's set to the host URL. -->
  <AppDomains>
    <AppDomain>https://localhost:5173</AppDomain>
  </AppDomains>
  
  <!-- Begin TaskPane Mode integration. This section is used if there are no VersionOverrides or if the Office client version does not support add-in commands. -->
  <Hosts>
    <Host Name="Workbook" />
  </Hosts>
  
  <DefaultSettings>
    <SourceLocation DefaultValue="https://localhost:5173" />
  </DefaultSettings>
  <!-- End TaskPane Mode integration.  -->
  
  <Permissions>ReadWriteDocument</Permissions>
  
  <!-- Begin Add-in Commands Mode integration. -->
  <VersionOverrides xmlns="http://schemas.microsoft.com/office/taskpaneappversionoverrides" xsi:type="VersionOverridesV1_0">
    
    <!-- The Hosts node is required. -->
    <Hosts>
      <!-- Each host can have a different set of commands. -->
      <!-- Excel host is Workbook, Word host is Document, and PowerPoint host is Presentation. -->
      <!-- Make sure the hosts you override match the hosts declared in the top section of the manifest. -->
      <Host xsi:type="Workbook">
        <!-- Form factor. Currently only DesktopFormFactor is supported. -->
        <DesktopFormFactor>
          <!-- PrimaryCommandSurface is the main Office Ribbon. -->
          <ExtensionPoint xsi:type="PrimaryCommandSurface">
            <!-- Use OfficeTab to extend an existing Tab. Use CustomTab to create a new tab. -->
            <OfficeTab id="TabHome">
              <!-- Ensure you provide a unique id for the group. Recommendation for any IDs is to namespace using your company name. -->
              <Group id="ExcelBotAI.Group">
                <!-- Label for your group. resid must point to a ShortString resource. -->
                <Label resid="ExcelBotAI.GroupLabel" />
                <!-- Icons. Required sizes 16,32,80, optional 20, 24, 40, 48, 64. Strongly recommended to provide all sizes for great UX. -->
                <!-- Use PNG icons. All URLs on the resources section must use HTTPS. -->
                <Icon>
                  <bt:Image size="16" resid="ExcelBotAI.Icon16" />
                  <bt:Image size="32" resid="ExcelBotAI.Icon32" />
                  <bt:Image size="80" resid="ExcelBotAI.Icon80" />
                </Icon>
                
                <!-- Control. It can be of type "Button" or "Menu". -->
                <Control xsi:type="Button" id="ExcelBotAI.TaskpaneButton">
                  <Label resid="ExcelBotAI.TaskpaneButton.Label" />
                  <Supertip>
                    <!-- ToolTip title. resid must point to a ShortString resource. -->
                    <Title resid="ExcelBotAI.TaskpaneButton.Label" />
                    <!-- ToolTip description. resid must point to a LongString resource. -->
                    <Description resid="ExcelBotAI.TaskpaneButton.Tooltip" />
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="ExcelBotAI.Icon16" />
                    <bt:Image size="32" resid="ExcelBotAI.Icon32" />
                    <bt:Image size="80" resid="ExcelBotAI.Icon80" />
                  </Icon>
                  
                  <!-- This is what happens when the command is triggered (E.g. click on the Ribbon). Supported actions are ExecuteFunction or ShowTaskpane. -->
                  <Action xsi:type="ShowTaskpane">
                    <TaskpaneId>ButtonId1</TaskpaneId>
                    <!-- Provide a URL resource id for the location that will be displayed on the task pane. -->
                    <SourceLocation resid="ExcelBotAI.Taskpane.Url" />
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>
        </DesktopFormFactor>
      </Host>
    </Hosts>
    
    <!-- You can use resources across hosts and form factors. -->
    <Resources>
      <bt:Images>
        <bt:Image id="ExcelBotAI.Icon16" DefaultValue="https://localhost:5173/icon-16.png" />
        <bt:Image id="ExcelBotAI.Icon32" DefaultValue="https://localhost:5173/icon-32.png" />
        <bt:Image id="ExcelBotAI.Icon80" DefaultValue="https://localhost:5173/icon-80.png" />
      </bt:Images>
      <bt:Urls>
        <bt:Url id="ExcelBotAI.Taskpane.Url" DefaultValue="https://localhost:5173" />
      </bt:Urls>
      <!-- ShortStrings max characters==125. -->
      <bt:ShortStrings>
        <bt:String id="ExcelBotAI.TaskpaneButton.Label" DefaultValue="ExcelBot AI" />
        <bt:String id="ExcelBotAI.GroupLabel" DefaultValue="AI Yardımcı" />
      </bt:ShortStrings>
      <!-- LongStrings max characters==250. -->
      <bt:LongStrings>
        <bt:String id="ExcelBotAI.TaskpaneButton.Tooltip" DefaultValue="AI destekli formül ve makro yardımcısını aç" />
      </bt:LongStrings>
    </Resources>
  </VersionOverrides>
  <!-- End Add-in Commands Mode integration. -->
  
</OfficeApp>
